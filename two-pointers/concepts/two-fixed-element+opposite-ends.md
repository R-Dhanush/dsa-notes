# Two fixed element + Opposite ends
- Same as [two-fixed-element+opposite-ends](two-fixed-element+opposite-ends.md) but with two fixed elements (two nested outer loops) before the inner converging pair.
- Requires sorted data — same two reasons as the single-fixed version (knowing which side to move, and skipping duplicates).
## Problems